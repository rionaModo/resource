module.exports = function (conf) {
  var ware,
      tpl = conf.tpl

  if(conf.debug){
    ware = function(err, req, res, next) {
      if(typeof err === "string"){
        
      }
      
    }
  }

  return function(err, req, res, next) {
    
  }


}